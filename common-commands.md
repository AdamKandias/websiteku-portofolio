git config --global user.name "AdamKandias"
git config --global user.email "emailku@gmail.com"


git init
git add .
git commit -m "pesan"
git status
git log
git push
git pull
git clone
git branch
git checkout -b
git merge